# CareerBuilder Analytics & KPI 트래킹 가이드

## 📊 NSM (North Star Metric) 정의

### Stage 1 (현재) - 사용자 확보 & 활성화

> **Primary NSM: 주간 성과 등록 완료 사용자 수 (WAU with Achievement)**

이 지표가 NSM으로 적합한 이유:
- 단순 방문이 아닌 **핵심 기능 사용**을 측정
- 나중에 "AI 분석 완료 사용자"로 자연스럽게 전환 가능
- 구독 전환의 선행 지표로 작용

### Stage 2 (추후) - 수익화

> **Future NSM: AI 기능 구독자 수**

---

## 🎯 KPI 체계

### Primary KPI (주요 지표)
| 지표 | 정의 | 측정 방법 | 목표 |
|------|------|-----------|------|
| WAU (성과 등록) | 주간 최소 1개 성과 등록 사용자 | `achievement_created` 이벤트의 unique user count | 주간 10% 성장 |

### Secondary KPIs (보조 지표)
| 지표 | 정의 | 측정 방법 |
|------|------|-----------|
| 신규 가입자 수 | 일/주간 신규 가입 완료 사용자 | `user_signup_completed` 이벤트 |
| Activation Rate | 가입 → 첫 성과 등록 전환율 (7일 이내) | 코호트 분석 |
| 사용자당 평균 성과 수 | 활성 사용자의 평균 성과 개수 | `achievement_created` / unique users |
| AI 요약 사용률 | 성과 등록 후 AI 요약 요청 비율 | `ai_summary_requested` / `achievement_created` |
| 리텐션율 | D1, D7, D30 재방문율 | 코호트 분석 |

---

## 🔄 핵심 퍼널

### 퍼널 1: 사용자 획득 → 활성화

```
[방문] → [회원가입 시작] → [회원가입 완료] → [첫 성과 등록] → [AI 요약 사용]
```

| 단계 | 이벤트명 | 전환율 목표 |
|------|----------|-------------|
| 방문 | `page_view (home)` | 100% (기준) |
| 회원가입 시작 | `user_signup_started` | 30% |
| 회원가입 완료 | `user_signup_completed` | 80% |
| 첫 성과 등록 | `achievement_created` (first) | 50% |
| AI 요약 사용 | `ai_summary_requested` | 40% |

### 퍼널 2: 성과 등록 플로우

```
[등록 페이지 진입] → [기본정보 입력] → [목표 입력] → [핵심성과 입력] → [저장 완료]
```

| 단계 | 이벤트명 | 핵심 속성 |
|------|----------|-----------|
| 페이지 진입 | `career_register_started` | `source` |
| 기본정보 입력 | `achievement_form_step_completed` | `step: 1` |
| 목표 입력 | `achievement_form_step_completed` | `step: 2` |
| 핵심성과 입력 | `achievement_form_step_completed` | `step: 3` |
| 저장 완료 | `achievement_created` | `word_count`, `has_sections` |

---

## 📈 대시보드 구성 권장

### Executive Dashboard
1. **WAU 트렌드** (주간 비교)
2. **신규 가입자 수** (일별)
3. **Activation Rate** (주간)
4. **AI 요약 사용률** (선행 지표)

### Product Dashboard
1. **퍼널 전환율** (단계별)
2. **성과 등록 완료율**
3. **페이지별 이탈률**
4. **사용자당 평균 성과 수**

### Growth Dashboard
1. **코호트별 리텐션**
2. **채널별 가입자 수**
3. **가입 방법별 전환율**

---

## 🔧 GA4 보고서 설정 가이드

### 맞춤 이벤트
GA4에서 다음 이벤트를 맞춤 정의로 등록:
1. `achievement_created` - 전환 이벤트로 표시
2. `user_signup_completed` - 전환 이벤트로 표시
3. `ai_summary_completed` - 전환 이벤트로 표시

### 맞춤 측정기준
| 측정기준 이름 | 이벤트 매개변수 | 범위 |
|---------------|-----------------|------|
| 가입 방법 | `method` | 이벤트 |
| 성과 유형 | `type` | 이벤트 |
| AI 제공자 | `provider` | 이벤트 |

---

## 🎯 Mixpanel 설정 가이드

### 핵심 이벤트 정의
1. `$signup` → `user_signup_completed`로 매핑
2. `Achievement Created` → 핵심 전환 이벤트
3. `AI Summary Requested` → 인게이지먼트 이벤트

### 유저 프로필 속성
| 속성명 | 설명 |
|--------|------|
| `$first_achievement_date` | 첫 성과 등록일 |
| `achievement_count` | 총 성과 개수 |
| `signup_method` | 가입 방법 |
| `ai_usage_count` | AI 요약 사용 횟수 |

### 퍼널 설정
1. **Activation Funnel**: Signup → First Achievement
2. **Engagement Funnel**: Achievement Created → AI Summary
